<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/color_fragment_container"
    android:layout_width="@dimen/color_dialog_width"
    android:layout_height="@dimen/color_dialog_height"
    android:layout_margin="@dimen/margin_normal"
    android:background="@color/colorGrayLight"
    android:padding="@dimen/padding_small">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rvDishIcon"
        android:layout_width="@dimen/dp_0"
        android:layout_height="@dimen/dp_0"
        app:layout_constraintBottom_toTopOf="@id/btnCancel"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnCancel"
        style="@style/TextViewTitleWhite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_small"
        android:layout_marginEnd="@dimen/margin_small"
        android:background="@null"
        android:padding="@dimen/padding_small"
        android:text="@string/cancel"
        android:textColor="@color/colorBluePrimaryLight"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent" />
</android.support.constraint.ConstraintLayout>