<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/item_unit_height"
    android:clickable="true"
    android:focusable="true">

    <ImageButton
        android:id="@+id/btnTick"
        android:layout_width="@dimen/tick_icon_width"
        android:layout_height="@dimen/tick_icon_height"
        android:layout_marginStart="@dimen/margin_small"
        android:background="@null"
        android:contentDescription="@string/image_description"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_tick" />

    <TextView
        android:id="@+id/tvUnit"
        style="@style/TextViewNormal"
        android:layout_width="@dimen/dp_0"
        android:layout_height="match_parent"
        android:layout_marginStart="@dimen/margin_small"
        android:layout_marginEnd="@dimen/margin_small"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center_vertical"
        app:layout_constraintBottom_toBottomOf="@id/btnTick"
        app:layout_constraintLeft_toRightOf="@id/btnTick"
        app:layout_constraintRight_toLeftOf="@id/btnEdit"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/btnEdit"
        android:layout_width="@dimen/item_unit_height"
        android:layout_height="@dimen/item_unit_height"
        android:layout_marginEnd="@dimen/margin_small"
        android:background="@null"
        android:contentDescription="@string/image_description"
        android:padding="@dimen/padding_small"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_edit" />

    <View
        android:layout_width="@dimen/dp_0"
        android:layout_height="@dimen/line_height"
        android:layout_marginTop="@dimen/margin_small"
        android:background="@color/colorTextGray"
        android:clickable="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />
</android.support.constraint.ConstraintLayout>